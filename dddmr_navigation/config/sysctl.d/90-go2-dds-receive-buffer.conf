# CycloneDDS requests this limit explicitly for sustained Hesai XT16 clouds.
# Keep net.core.rmem_default unchanged so unrelated UDP sockets retain the
# platform default unless they also opt into a larger receive buffer.
net.core.rmem_max = 16777216
