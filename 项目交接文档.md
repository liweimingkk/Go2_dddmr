# Go2 + XT16 DDDMR 导航项目交接文档

> 最后更新：2026-08-25
>
> 当前仓库：/home/kkkkkkq/new22/new2
>
> 导航工作区：/home/kkkkkkq/new22/new2/dddmr_navigation
>
> 编写前代码基线：分支 agent/fix-go2-time-sync，提交 23e879f
>
> 适用硬件：Unitree Go2 EDU + Hesai XT16
>
> 适用系统：Ubuntu 22.04 + ROS 2 Humble + Docker

## 0. 交接结论

本仓库已经具备 Go2 + XT16 的三维建图、地图服务、三维定位、全局规划、局部规划、点到点导航 dry-run、受监督 Sport 控制链和录制路线巡航框架。当前推荐架构是：

    Docker：
      DDDMR 建图、定位、感知、规划、RViz

    Host：
      Go2 ROS 2 / Unitree 消息环境
      仅在明确批准的监督运动测试中运行 Sport adapter

项目当前不能按“无人值守自主配送已完成”交接。更准确的状态是：

| 能力 | 当前结论 |
| --- | --- |
| XT16 数据契约和 Docker 运行框架 | 已建立，历史 live 预检通过 |
| 建图和地图保存 | 主链路可用，本机存在多批地图工件 |
| 口部雷达地面补充 | 静止固定 ROI 方案已有离线证据，不是在线障碍融合 |
| 三维定位和全局重定位 | 软件和离线回放已实现，当前现场重新定位验收仍需补做 |
| 静态墙体与全局规划 | 关键缺陷已修复并有合成验证，真实地图现场复验未闭环 |
| 局部避障 | 已有多轮改进和调试 bag，缺少足够的重复现场成功证据 |
| 点到点实机导航 | 有受监督短目标记录，但没有重复到点和量化终点误差证据 |
| 录制路线巡航 | 录制、校验、跟踪、三维走廊和安全入口已实现，户外正式路线验收待完成 |
| 室内外完整任务状态机 | 未形成完整交付模块 |
| 机械臂咖啡交付 | 本仓库未实现完整控制、互锁和取杯检测 |
| 端到端咖啡配送 | 未完成统计验收，不可宣称可交付无人运行 |

最重要的安全边界：

- 默认只允许只读检查、建图、RViz 和 dry-run。
- 未经明确批准，不发布 /cmd_vel、/api/sport/request 或 /lowcmd。
- 真实运动必须现场有人监督，原厂遥控器或物理急停可立即接管。
- 不修改 Go2 网络、服务、启动项、固件、标定或 /home/unitree/xt16_ws。
- live 运行前必须先证明真实点云样本、TF、里程计、定位状态和无残留进程。
- “节点能启动”“有全局路径”“dry-run 有速度”都不等于实机可安全运行。

## 1. 当前工作区快照

### 1.1 Git 状态

文档编写前状态：

    branch: agent/fix-go2-time-sync
    HEAD:   23e879f Document Go2 navigation safety review
    remote: origin/agent/fix-go2-time-sync

已有两处用户未提交修改，本次交接文档不应覆盖或误提交它们：

    dddmr_navigation/src/dddmr_lego_loam/lego_loam_bor/src/lego_loam_bag_node.cpp
    方案.md

后续提交必须显式列出文件，不要使用 git add -A。

当前全工作区运行 ./scripts/check-repository.sh 会因已有 方案.md 的第 3 行尾随空格和文件末尾空行而失败。该失败不属于交接文档变更；除非先确认这份用户文件的预期内容，否则不要为了让检查变绿而擅自格式化它。

### 1.2 本机运行状态

2026-08-25 检查时：

- 没有正在运行的 Docker 容器。
- 根目录有两个旧的忽略文件：.current_live_rviz_test 和 .current_navigation_run，时间为 2026-07-01。它们只是旧标记，不代表进程仍在运行。
- Host 已安装 Docker 29.6.1、Ubuntu 22.04.5 和 ROS 2 Humble 命令环境。
- 当前 net.core.rmem_default 和 net.core.rmem_max 都是 212992 字节，低于项目要求的 16777216 字节。
- 当前尝试创建 CycloneDDS 节点时出现接收缓冲区不足并创建 domain 失败。因此恢复 live ROS 前，应先解决 Host UDP 接收缓冲区问题。

只读检查：

    cd /home/kkkkkkq/new22/new2/dddmr_navigation
    ./scripts/check_go2_dds_receive_buffers.sh

脚本会输出建议的 sysctl 值。修改 Host 系统参数需要由当前机器管理员确认，不要在交接时静默修改系统设置。

### 1.3 本机运行工件

以下目录存在，但被 Git 忽略，不会随 git clone 自动迁移：

    /home/kkkkkkq/new22/new2/bags
    /home/kkkkkkq/new22/new2/offline_maps
    /home/kkkkkkq/new22/new2/dddmr_navigation/run_logs
    /home/kkkkkkq/new22/new2/dddmr_navigation/report_packages
    /home/kkkkkkq/new22/new2/dddmr_navigation/review_packets

当前导航配置选择的地图：

    bags/go2_xt16_mouth_mapping_20260714_153136_map_2026_07_14_07_31_36

该目录在本机存在，包含：

    map.pcd
    ground.pcd
    edges.pcd
    poses.pcd
    pcd/<keyframe>_feature.pcd
    pcd/<keyframe>_ground.pcd
    pcd/<keyframe>_surface.pcd

本机路线目录已有：

    bags/routes/flat_route_01.json
    bags/routes/flat_route_02.json
    bags/routes/go2_xt16_indoor_reverse_test.json

接手后第一件事应备份 bags、run_logs、review_packets 和现场配置。源码、地图、路线、bag 和验收日志必须作为两套不同资产管理。

## 2. 项目目标与范围

### 2.1 导航工程目标

本仓库基于 DDDMR Navigation，为 Go2 EDU + XT16 提供：

1. XT16 在线和 rosbag2 离线三维建图。
2. LeGO-LOAM pose graph、完整点云地图和地面地图保存。
3. 基于 MCL 的三维定位和全图重定位。
4. 静态地图、实时障碍和禁行/限速图层。
5. 地面点云图上的三维 A* 全局规划。
6. 轨迹采样、碰撞检测、路径跟踪和恢复行为。
7. 点到点导航状态机。
8. 人工录制三维路线的受限走廊巡航。
9. Docker 内 DDDMR 与 Host 上 Unitree Sport API 的安全隔离。

### 2.2 咖啡配送目标

业务方案见 [方案.md](方案.md)。目标链路是：

    装载咖啡
      -> 户外固定路线
      -> 入口停车和定位确认
      -> 室内固定交付点
      -> 机械臂托盘交付
      -> 取杯确认
      -> 返程或人工回收

当前仓库主要覆盖导航部分。订单、机械臂、杯托传感器、底盘与机械臂互锁、客户确认、室内外任务编排、交付记录等仍需单独实现和联调。

## 3. 仓库结构

    new2/
    ├── AGENTS.md
    ├── README.md
    ├── 项目交接文档.md
    ├── 方案.md
    ├── bags/                         # 地图、bag、路线，Git 忽略
    ├── offline_maps/                 # 历史离线地图，Git 忽略
    ├── scripts/
    │   ├── check-repository.sh
    │   └── submit-change.sh
    └── dddmr_navigation/
        ├── AGENTS.md                 # 实机与运动安全边界
        ├── docs/                     # 运行手册、评审、验收说明
        ├── scripts/                  # Go2/XT16 构建、预检和启动脚本
        ├── dddmr_docker/             # Dockerfile 和上游镜像入口
        └── src/
            ├── dddmr_lego_loam/      # 建图、特征、pose graph
            ├── dddmr_pg_map_server/  # 地图发布
            ├── dddmr_mcl_3dl/        # 三维 MCL 和全局重定位
            ├── dddmr_perception_3d/  # 静态/动态三维感知
            ├── dddmr_global_planner/ # 全局 A*
            ├── dddmr_local_planner/  # 轨迹、critics、恢复
            ├── dddmr_p2p_move_base/  # 点到点导航状态机
            ├── dddmr_route_navigation/ # 录制路线控制器
            ├── dddmr_beginner_guide/ # Go2 launch、YAML、RViz、安全门
            └── dddmr_rviz_tools/     # 3D 初始位姿和目标等插件

常用入口：

| 用途 | 文件 |
| --- | --- |
| Docker 总入口 | dddmr_navigation/scripts/dddmr_docker_go2_xt16.sh |
| XT16 在线配置 | dddmr_navigation/src/dddmr_lego_loam/lego_loam_bor/config/loam_go2_xt16_live.yaml |
| 点到点导航 launch | dddmr_navigation/src/dddmr_beginner_guide/launch/go2_xt16_navigation.launch |
| 点到点导航参数 | dddmr_navigation/src/dddmr_beginner_guide/config/go2_xt16_navigation.yaml |
| 全局重定位参数 | dddmr_navigation/src/dddmr_beginner_guide/config/go2_xt16_relocalization.yaml |
| 录制路线 launch | dddmr_navigation/src/dddmr_beginner_guide/launch/go2_xt16_recorded_route_navigation.launch |
| 录制路线参数 | dddmr_navigation/src/dddmr_route_navigation/config/go2_xt16_recorded_route.yaml |
| Sport 实机说明 | dddmr_navigation/docs/go2_xt16_sport_live_runbook.md |

## 4. 硬件、软件、网络和数据契约

### 4.1 推荐环境

| 项目 | 推荐值 |
| --- | --- |
| 操作系统 | Ubuntu 22.04 LTS amd64 |
| ROS | ROS 2 Humble |
| RMW | rmw_cyclonedds_cpp |
| 容器 | Docker Engine |
| 基础镜像 | dddmr:x64 |
| Go2 薄封装镜像 | dddmr_go2_xt16:x64 |
| ROS_DOMAIN_ID | 0 |

不推荐在 Host 直接构建完整 DDDMR。该工程固定依赖 PCL 1.15.0、GTSAM 4.2a9、GCC 13 和特定 Humble 组合，混用 Host 与 Docker 二进制容易出现 allocator、double free、exit -8 或 exit -11。

### 4.2 网络约定

历史记录中的默认值：

| 项目 | 值 |
| --- | --- |
| Go2 controller/data-plane | 192.168.123.161 |
| Go2 SSH/DDS 路由目标 | 192.168.123.18 |
| Host 有线地址 | 192.168.123.222/24 |
| DDS 网卡 | 通过到 GO2_DDS_IP 的路由自动判断 |

现场先只读检查：

    ip -br addr
    ip route
    ping -c 2 -W 1 192.168.123.161
    ping -c 2 -W 1 192.168.123.18
    ip route get 192.168.123.18

若自动选错网卡：

    export GO2_NET_IFACE=<连接 Go2 的有线网卡>

### 4.3 XT16 点云合同

当前正式导航使用 32000 点、约 10 Hz 模式：

| 字段 | 期望 |
| --- | --- |
| topic | /lidar_points |
| type | sensor_msgs/msg/PointCloud2 |
| frame_id | hesai_lidar |
| width × height | 32000 × 1 |
| point_step | 26 |
| fields | x, y, z, intensity, ring, timestamp |
| ring | UINT16，范围 0..15 |
| 每 ring 点数 | 2000 |
| 帧内 timestamp 跨度 | 约 0.1 s |
| 接收频率 | 约 10 Hz |

64000 点模式实测约 5 Hz。若切换到该模式，必须同时将水平投影列数改为 4000。不能让 2000 点/ring 的数据进入 4000 列 range image。

### 4.4 其他关键输入

| 数据 | topic | 说明 |
| --- | --- | --- |
| Go2 原始里程计 | /utlidar/robot_odom | 进入标准化节点 |
| 标准里程计 | /dddmr_go2/robot_odom_standard | 默认 odom -> base_link，当前 yaw 旋转为 0 |
| 口部雷达点云 | /utlidar/cloud_base | 当前只用于近场地面补充 |
| 点到点 dry-run 速度 | /dddmr_go2/dry_run_cmd_vel | 与真实控制隔离 |
| 安全门输出 | /dddmr_go2/safe_cmd_vel | 仅 TRACKING 且命令新鲜时非零 |
| 真实 Unitree 请求 | /api/sport/request | 只允许受监督 Host adapter 发布 |

### 4.5 TF 约定

    map
      -> odom
        -> base_link
          -> base_footprint
          -> go2_imu
            -> hesai_lidar

坐标定义：

- base_link +X：机器狗物理前方。
- base_link +Y：左方。
- base_link +Z：上方。

当前静态外参起点：

| 变换 | 参数 |
| --- | --- |
| base_link -> base_footprint | z = -0.24 m |
| base_link -> go2_imu | x = -0.02557, z = 0.04232 |
| go2_imu -> hesai_lidar | x = 0.1710, z = 0.0908, yaw = +1.5707963 |

PUBLISH_STATIC_TF=true 只应用于现场 TF 缺失时的感知/建图验证。若实机已经发布同名 TF，不能重复发布。

## 5. 首次启动

### 5.1 设置路径

从仓库根目录执行：

    cd /home/kkkkkkq/new22/new2
    export REPO_ROOT="$(git rev-parse --show-toplevel)"
    export DDDMR_WS="$REPO_ROOT/dddmr_navigation"
    export DDDMR_BAGS_DIR="$REPO_ROOT/bags"
    export ROS_DOMAIN_ID=0
    cd "$DDDMR_WS"

不要照抄旧文档中的 /home/lin 路径。部分历史验收文件仍保留绝对路径，迁移后需要重新生成证据或明确映射。

### 5.2 检查工作区和残留运行时

    git status --short --branch
    docker ps --format '{{.Names}} {{.Status}} {{.Image}}'
    ps -ef | rg 'lego_loam|mcl_3dl|p2p_move_base|sport_cmd_vel|recorded_route'

若发现旧导航或 Sport adapter，使用对应启动脚本的 --stop/--disable，而不是直接粗暴杀死所有 ROS 进程。

### 5.3 检查 Host DDS 缓冲区

    ./scripts/check_go2_dds_receive_buffers.sh

项目要求 net.core.rmem_default 和 net.core.rmem_max 均不低于 16777216。当前机器未达到要求，这是恢复 live ROS 的已知阻塞项。

### 5.4 构建镜像

    ./scripts/dddmr_docker_go2_xt16.sh build-go2-image

脚本会在缺少基础镜像时先构建 dddmr:x64，再构建 dddmr_go2_xt16:x64。首次构建会编译 PCL/GTSAM，耗时较长。

检查：

    docker image inspect dddmr:x64 >/dev/null
    docker image inspect dddmr_go2_xt16:x64 >/dev/null

### 5.5 只读 XT16 预检

    ./scripts/dddmr_docker_go2_xt16.sh preflight --samples 3 --timeout 10

验收必须看真实样本，不只看 publisher：

- frame_id 是 hesai_lidar。
- 点云字段齐全。
- ring 范围是 0..15。
- 每 ring 约 2000 点。
- 总宽度约 32000。
- 频率约 10 Hz。

预检不通过时不要启动建图或导航。

## 6. 构建与运行

### 6.1 构建 LeGO-LOAM

    cd "$DDDMR_WS"
    ./scripts/dddmr_docker_go2_xt16.sh build-lego

编译产物写入 Git 忽略目录：

    .docker_go2_xt16_build
    .docker_go2_xt16_install
    .docker_go2_xt16_log

### 6.2 在线 no-motion 建图烟测

先保证 Go2 静止：

    RUN_SECONDS=20 RVIZ=false PUBLISH_STATIC_TF=true \
      ./scripts/dddmr_docker_go2_xt16.sh mapping

若实机 TF 完整：

    RUN_SECONDS=20 RVIZ=false PUBLISH_STATIC_TF=false \
      ./scripts/dddmr_docker_go2_xt16.sh mapping

观察：

    /ground_cloud
    /segmented_cloud_pure
    /laser_odom_to_init
    /lego_loam_map

验收：

- lego_loam 不崩溃。
- 第一帧地面不是空心。
- ground_cloud 和 segmented_cloud_pure 持续更新。
- TF 无重复发布和大幅跳变。

地面空心时必须保持静止，优先检查 patch_first_ring_to_baselink 和 ground FOV，不要边运动边调。

### 6.3 口部雷达地面融合

口部雷达不是默认在线障碍源。当前流程只把固定 ROI 内的口部点加入 ground_cloud，不加入 segmented_cloud_pure。

先测时钟偏移：

    source /opt/ros/humble/setup.bash
    source scripts/setup_go2_dds_env.sh
    python3 scripts/measure_go2_mouth_xt16_time_offset.py \
      --mouth-topic /utlidar/cloud_base \
      --xt16-topic /lidar_points \
      --duration 8

再做静止验证：

    RUN_SECONDS=20 RVIZ=false PUBLISH_STATIC_TF=true \
      MOUTH_MAX_TIME_DIFF=0.03 \
      ./scripts/run_go2_xt16_mouth_validation.sh

正式建图和保存入口：

    MAPPING_SECONDS=120 RVIZ=true MAP_RVIZ=true \
      ./scripts/run_go2_xt16_mouth_mapping_save_to_nav.sh

该脚本可能保存地图并更新 go2_xt16_navigation.yaml 中的 pose_graph_dir，执行前必须检查 Git diff 和输出目录。地图晋升前必须保留时间偏移、融合计数、配置快照和日志。

当前口部固定 ROI 起点：

| 项目 | 值 |
| --- | --- |
| filter frame | base_link |
| z | -0.42 .. -0.18 m |
| x | 0.05 .. 3.0 m |
| abs(y) | <= 1.2 m |

这个 ROI 只适用于静止或姿态变化很小的场景，不能当作坡道、台阶、跌落或低障碍可通行性分类器。

### 6.4 离线 bag 建图

    CONFIG_FILE=/root/dddmr_navigation/src/dddmr_lego_loam/lego_loam_bor/config/loam_bag_go2_xt16_20260704_081247.yaml \
    DDDMR_MAPPING_DIR=/root/dddmr_bags/go2_xt16_offline_map_ \
      ./scripts/dddmr_docker_go2_xt16.sh mapping-bag

离线配置必须与 bag 的实际点数、频率、topic、frame 和里程计时间基准一致。历史 64000 点 bag 不应使用当前 2000 列 live 配置。

### 6.5 构建完整导航链

    ./scripts/dddmr_docker_go2_xt16.sh build-navigation

主要构建：

- lego_loam_bor
- dddmr_pg_map_server
- mcl_3dl
- perception_3d
- global_planner
- local planner 子包
- p2p_move_base
- dddmr_route_navigation
- dddmr_beginner_guide
- RViz 插件

### 6.6 点到点导航 dry-run

推荐入口：

    RUN_SECONDS=20 RVIZ=false PUBLISH_STATIC_TF=true \
      ./scripts/dddmr_docker_go2_xt16.sh navigation-dry-run

交互式 RViz：

    RVIZ=true PUBLISH_STATIC_TF=true \
      ./scripts/dddmr_docker_go2_xt16.sh navigation-dry-run

也可使用：

    ./scripts/run_go2_xt16_navigation_test.sh --dry-run

安全链：

    p2p_move_base /cmd_vel
      -> /dddmr_go2/dry_run_cmd_vel
      -> go2_nav_cmd_gate
      -> /dddmr_go2/safe_cmd_vel
      -> dry-run Sport logger

dry-run 不应创建真实 /api/sport/request publisher。

RViz 和 topic 重点观察：

    /map1/mapcloud
    /map1/mapground
    /sub_mapcloud
    /sub_mapground
    /localization_status
    /localization_quality
    /weighted_ground
    /global_path
    /awared_global_path
    /perception_3d_ros/dGraph
    /dddmr_go2/p2p_decision
    /dddmr_go2/dry_run_cmd_vel
    /dddmr_go2/safe_cmd_vel

验收顺序：

1. 地图发布完整。
2. TF 和机器人位置正确。
3. 定位进入 TRACKING。
4. 起点、目标点落在有效地面上。
5. 全局路径不穿墙。
6. 局部规划不把地面/自身误判为障碍。
7. dry-run 速度方向、幅值和决策状态一致。
8. 清除目标或退出后速度归零。
9. 停止后没有残留容器或 adapter。

### 6.7 录制路线 dry-run

离线生成或校验路线：

    cd "$REPO_ROOT"
    python3 dddmr_navigation/src/dddmr_route_navigation/scripts/route_tool.py \
      inspect bags/routes/go2_xt16_indoor_reverse_test.json

只准备工件、不连接 ROS：

    cd "$DDDMR_WS"
    ./scripts/run_go2_xt16_recorded_route_dry_run.sh --prepare-only

启动无运动路线栈：

    ./scripts/run_go2_xt16_recorded_route_dry_run.sh --start

状态和控制：

    ./scripts/run_go2_xt16_recorded_route_dry_run.sh --status
    ./scripts/run_go2_xt16_recorded_route_dry_run.sh --enable
    ./scripts/run_go2_xt16_recorded_route_dry_run.sh --disable
    ./scripts/run_go2_xt16_recorded_route_dry_run.sh --stop

即使执行 --enable，输出仍应停留在 dry-run topic，不发布真实 Sport 请求。

录制新路线时，先启动定位但保持路线控制器 DISABLED，再由操作员用原厂遥控器人工走路线：

    ROUTE_ID=park_route_a \
      ./scripts/record_go2_xt16_recorded_route.sh

保存后检查：

    python3 src/dddmr_route_navigation/scripts/route_tool.py \
      inspect "$DDDMR_BAGS_DIR/routes/park_route_a.json"

路线 JSON 包含 x/y/z、四元数和地图工件 SHA-256。路线与地图必须成对冻结。

### 6.8 真实运动入口

本节只是交接说明，不代表批准运动。只有用户明确批准、现场具备独立安全员和急停后才可执行。

点到点监督 live 的顺序：

1. 通过 dry-run。
2. 运行只读 readiness：

       ./scripts/check_go2_xt16_sport_live_readiness.sh

3. 在空旷区域运行受监督低速 Sport 探针，保留 summary。
4. 将 summary 交给混合运行入口：

       GO2_NAV_LIVE_CONFIRM=I_AM_SUPERVISING_GO2_NAV \
       GO2_SPORT_PROBE_SUMMARY=/tmp/go2_sport_adapter_live_YYYYMMDD_HHMMSS_summary.env \
         ./scripts/run_go2_xt16_navigation_supervised_live.sh --live

录制路线监督 live 使用独立入口：

    ./scripts/run_go2_xt16_recorded_route_supervised_live.sh --check

只有 --check 通过后，才按 [录制路线说明](dddmr_navigation/docs/go2_xt16_recorded_route_navigation.md) 准备路线专用确认短语、Sport 探针和现场参数。

不要把 go2_xt16_recorded_route_navigation.launch 中的真实 Sport adapter 直接打开，也不要绕过 confirmation、probe summary、TRACKING、决策状态、命令超时和清理 trap。

## 7. 算法框架

### 7.1 总体数据流

    XT16 /lidar_points
      -> LeGO-LOAM imageProjection
      -> 地面/非地面分割和特征提取
      -> featureAssociation
      -> mapOptimization / pose graph
      -> map.pcd + ground.pcd + keyframes

    pose graph
      -> dddmr_pg_map_server
      -> mapcloud + mapground + key poses

    live XT16 features + odom + pose graph
      -> mcl_3dl
      -> map -> odom
      -> localization_status

    mapcloud/mapground + segmented_cloud_pure
      -> perception_3d local/global
      -> static/dynamic graph

    goal + graph
      -> global_planner A*
      -> global_path / awared_global_path

    global path + odom + collision graph
      -> local_planner
      -> p2p_move_base
      -> dry_run_cmd_vel
      -> localization/timeout/decision gate
      -> safe_cmd_vel
      -> dry-run logger 或受监督 Host Sport adapter

录制路线模式会绕过 global_planner、P2PMoveBase 和 recovery，用人工路线片段直接驱动 Local Planner，同时增加三维路线走廊约束。

### 7.2 建图：LeGO-LOAM-BOR

核心位置：

    src/dddmr_lego_loam/lego_loam_bor

主要阶段：

1. imageProjection 将点云投影成 16 × 2000 range image。
2. 根据垂直角、相邻点关系和地面 FOV 提取地面。
3. 对非地面区域做连通分割，输出 segmented_cloud_pure。
4. featureAssociation 计算边缘/平面特征和帧间激光里程计。
5. mapOptimization 将关键帧加入 pose graph，做局部地图优化和可选闭环。
6. 保存 map、ground、edges、poses 和每个 keyframe 的 feature/ground/surface。

当前 live 参数：

| 参数 | 值 |
| --- | --- |
| num_vertical_scans | 16 |
| num_horizontal_scans | 2000 |
| vertical_angle_bottom/top | -15 / +15 deg |
| scan_period | 0.1 s |
| patch_first_ring_to_baselink | true |
| enable_loop_closure | false |
| use_omp_ground_voxel_filter | false |

第一轮 live 烟测关闭闭环和 OMP ground voxel filter，是为了降低运行时不稳定变量，不代表最终地图一定不需要闭环。

### 7.3 时间同步

XT16、Go2 odom 和口部雷达可能来自不同时间基准。当前代码加入了：

- odom/XT16 偏移测量。
- mouth/XT16 偏移测量。
- receipt-time 匹配工具。
- 时钟阶跃和一致性检查。
- receipt_sync_utils 单元测试。

正式建图前应重新测量，不要沿用旧机器固定偏移。receipt_time 模式当前默认使用约 0.03 s 的到达时间窗口；只有诊断几何链路时才可临时放宽。运动建图必须回到现场测量支持的严格值，不能把大窗口当作同步修复。

### 7.4 地图服务

dddmr_pg_map_server 读取 pose_graph_dir，并发布：

    map1/mapcloud
    map1/mapground
    key poses / submaps

当前配置地图：

    /root/dddmr_bags/go2_xt16_mouth_mapping_20260714_153136_map_2026_07_14_07_31_36

容器内 /root/dddmr_bags 映射到 Host 的 $DDDMR_BAGS_DIR。

### 7.5 三维 MCL 和全局重定位

mcl_3dl 使用 live feature cloud、里程计、完整地图、地面地图和 key poses 估计 map -> odom。

状态机：

    UNINITIALIZED
      -> LOCALIZING
      -> TRACKING
      -> LOST
      -> 全图搜索
      -> LOCALIZING

只有 TRACKING 允许命令门转发非零速度。

自动全图搜索：

1. 在 key pose/地图范围采样位置。
2. 为每个位置测试多个 yaw。
3. 用稀疏 live 特征给候选评分。
4. 保留高分候选并展开粒子。
5. 用连续观测判断主模态质量、概率质量、XY/yaw 方差和位姿稳定性。
6. 连续满足阈值后进入 TRACKING。

当前策略还加入 ESS、KLD 粒子数估计和单帧最大缩减比例，避免模糊场景过早丢弃候选。

历史离线回放验证过：

- keyframe 20 可进入 TRACKING。
- 断流后进入 LOST 且速度门保持零。
- 切换到远处 keyframe 35 后可重新全图搜索并 TRACKING。

这不是当前现场验收，接手后仍需在无目标、无运动条件下做 live 重定位复验。

### 7.6 三维感知

perception_3d 分为 local 和 global 两套：

- StaticLayer：用 mapcloud 对 mapground 节点施加静态障碍。
- MultiLayerSpinningLidar：用 segmented_cloud_pure 做在线 marking/clearing。
- DynamicGraph：生成给规划器使用的三维图。
- 局部层：服务 Local Planner，窗口小、更新快。
- 全局层：服务 A*，保留完整地图连通性和权重。

当前关键值：

| 参数 | 值 |
| --- | --- |
| local/global inscribed_radius | 0.40 m |
| static_obstacle_xy_radius | 0.40 m |
| local/global xy_resolution | 0.05 m |
| height_resolution | 0.05 m |
| XT16 expected_sensor_time | 0.1 s |
| 局部 marking 高度 | -0.20 .. 0.60 m |

已知边界：

- 静态地图以特征点表示，光滑墙面可能稀疏。
- 地面图仍缺显式坡度、台阶、跌落和跨层连接限制。
- 口部雷达当前不提供在线障碍 marking/clearing。

### 7.7 全局规划

global_planner 在 mapground/dGraph 上进行点云图 A*：

- 起点和目标投影到附近有效地面节点。
- 邻域搜索生成后继。
- 静态/动态障碍修改节点强度或可通行状态。
- 输出 global_path 和 dynamic-window-aware path。

已修复的关键问题：

- 稀疏节点扩大半径搜索时，邻居 index 与距离数组不一致。
- 静态墙点没有被赋予不可穿越代价。
- 静态障碍距离信息与清障半径不一致。

合成墙测试通过不等于选定真实地图已通过；真实 start-to-door 目标仍需 no-Sport 复验。

### 7.8 局部规划和点到点状态机

Local Planner 包含：

- 差速/原地旋转/Go2 heading arc 轨迹生成器。
- 碰撞、路径粘附、pure pursuit、朝向全局路径等 critics。
- 三维 cuboid 碰撞包络。
- 原地转向方向迟滞，减少左右反复抖动。
- 连续失败确认、等待和恢复策略。

当前点到点配置：

| 参数 | 当前值 |
| --- | --- |
| controller_frequency | 10 Hz |
| xy_goal_tolerance | 0.25 m |
| yaw_goal_tolerance | 0.15 rad |
| forward max/min | 0.40 / 0.30 m/s |
| yaw max/min | 0.50 / 0.40 rad/s |
| Go2 cuboid X | -0.35 .. 0.42 m |
| Go2 cuboid Y | -0.30 .. 0.30 m |
| Go2 cuboid Z | -0.20 .. 0.60 m |

P2PMoveBase 协调全局规划、局部规划、初始朝向、目标朝向、等待和恢复。Sport adapter 只允许规划器处于允许的控制/对齐决策且消息新鲜时输出 Move，其余状态输出 StopMove。

### 7.9 录制路线算法

dddmr_route_navigation 不运行全局 A*，而是：

1. 从人工路线 JSON 读取三维 pose 序列。
2. 去除重复点并按固定间距重采样。
3. 在当前进度附近找最近路线点，禁止任意跳到远段。
4. 只向前推进路线进度。
5. 取当前点前后的局部路线窗口送给 Local Planner。
6. 用 RouteCorridorModel 拒绝离开三维走廊的预测轨迹。
7. 无安全轨迹时停车；默认 5 秒仍未恢复进入 FAULT。
8. 每次加载新路线后回到 DISABLED，必须显式 enable。

当前默认约束：

| 参数 | 值 |
| --- | --- |
| 路线重采样间距 | 0.10 m |
| 最大输入点间隔 | 2.0 m |
| 路线水平走廊 | 0.60 m |
| 路线高度走廊 | 0.35 m |
| 控制器 max x/y/yaw | 0.35 / 0 / 0.50 |
| blocked timeout | 5 s |

该模式允许走廊内短距离局部绕障，但不会越过走廊全局重规划。坡道/楼梯的姿态和落脚由 Go2 本体负责，软件没有自动切换步态或身体高度。

## 8. 当前任务进展

### 8.1 已完成或已有明确代码证据

1. Docker 双层镜像与可移植 wrapper。
2. XT16 16 × 2000、10 Hz live 配置和只读预检。
3. Go2 odom 标准化与轴向检查工具。
4. XT16 单雷达建图、离线 bag 建图和 pose graph 保存。
5. 口部雷达 receipt-time 同步与地面补充验证工具。
6. 地图服务、MCL、自动全局重定位和命令联锁。
7. 静态墙体不可穿越、A* 稀疏邻域和感知分辨率修复。
8. 点到点 dry-run 和 Docker/Host 混合监督运行架构。
9. 命令超时、速度限幅、规划决策 gate、StopMove 和 bounded cleanup。
10. 局部障碍处理、转向迟滞和恢复状态门控改进。
11. 路线 JSON、地图指纹、路线录制、三维进度跟踪、走廊 critic。
12. 录制路线 dry-run 与独立的受监督 live 入口。
13. 当前本机存在最新配置地图和三条路线 JSON。

### 8.2 历史实机/离线证据

| 日期 | 证据 |
| --- | --- |
| 2026-07-02 | 口部地面补充 bag 中 mouth_ground、ground、segmented_pure 输出和几何比例通过 |
| 2026-07-04 | RViz 前方轴向、TF tilt、raw/standard odom 轴向和受监督短目标记录为 PASS |
| 2026-07-10 | live XT16 32000 点、2000 点/ring、约 10 Hz 数据合同确认 |
| 2026-07-10 | 2000 列 range image 恢复 segmented_cloud_pure 约 10 Hz |
| 2026-07-10 | 静态墙体与稀疏 A* 修复完成合成验证 |
| 2026-07-11 | 多轮近障碍、人员、恢复门复现 bag 被保存在 run_logs |
| 2026-07-16 | 全局重定位自适应粒子策略和单元测试加入 |
| 2026-07-17 | 录制路线跟踪、走廊、工具和安全启动链加入 |
| 2026-07-20 | 当前安全评审和墙穿越案例文档归档 |

历史证据文件中存在 /home/lin 绝对路径。若证据文件指向的原始日志在当前机器不存在，不能只凭 PASS 文本继续宣称当前环境通过。

### 8.3 尚未完成

| 项目 | 缺口 | 完成标准 |
| --- | --- | --- |
| 当前机器 live DDS | UDP 接收缓冲区不足，当前 ROS domain 创建失败 | 缓冲区检查 PASS，preflight 连续收到真实样本 |
| 最新代码现场回归 | 多项修复后缺同一配置的完整现场复验 | 记录 commit、地图 hash、YAML、bag 和结果 |
| 墙体/窄通道 | 合成测试通过，选定地图真实目标仍待复验 | no-Sport 路径绕墙或明确无路，之后再监督实跑 |
| 坡道/台阶/跌落 | 地面图无显式坡度、step/drop 约束 | 建模、单元测试、回放和现场分级验收 |
| 口部雷达在线安全 | 只补 ground，不做 obstacle/clearing/drop | 明确 mapping-only 或设计独立安全感知输出 |
| 地图晋升 | 未强制绑定同步测量和融合统计 | 每张地图附配置、日志、统计和 hash 后才能切换导航 |
| 重复到点 | 没有多次 goal reached、终点误差和最小间距统计 | 代表性目标多次成功并保留机器可读结果 |
| 录制路线现场验收 | 框架和室内测试路线已有，公园路线和完整实跑证据缺失 | 路线/地图冻结，平地短段后逐级到坡道和全程 |
| 室内外切换 | 尚无完整任务状态机 | 停车、地图切换、重定位、失败回退均有测试 |
| 机械臂交付 | 仓库无完整模块 | 接口、固定轨迹、杯检、双向互锁、异常处理和循环测试 |
| 端到端配送 | 无统计结果 | 空载、配重、常温水、咖啡分级测试和连续成功记录 |

### 8.4 仍然有效的安全评审问题

来自 [导航评审基线](dddmr_navigation/docs/go2_xt16_navigation_review_20260710.md)：

- NAV-REV-01：local LiDAR 分辨率已修，现场 dry-run 待确认。
- NAV-REV-02：A* 稀疏邻域已修并有合成验证。
- NAV-REV-03：真实 Sport publisher 生命周期已缓解，完整现场复验待补。
- NAV-REV-04：口部雷达不是在线障碍源，仍开放。
- NAV-REV-05：口部 ground ROI 是静止方案，仍开放。
- NAV-REV-06：缺坡度、台阶、跌落和跨层约束，仍开放。
- NAV-REV-07：0.40 m clearance 是候选参数，现场窄通道验收未闭环。
- NAV-REV-08：XT16 ring 和硬件逐点 timestamp 未被完整用于投影/deskew，仍开放。
- NAV-REV-09：地图晋升证据不足，仍开放。
- NAV-REV-10：没有重复到点证据，仍开放。
- NAV-REV-11：历史 poses.pcd 的 time 字段可能未初始化，仍开放。
- NAV-REV-12：2000/4000 列不一致已修，live 回归待持续确认。

在这些问题关闭前，状态保持“受监督研发验证”，不能标记为无人自主运行 READY。

## 9. 建议接手顺序

### P0：保护资产

1. 备份 bags、run_logs、review_packets、配置和路线 JSON。
2. 记录当前 branch、commit、dirty files 和地图 hash。
3. 不清理未知 build/log 目录，不覆盖同名路线或地图。

### P1：恢复当前机器只读能力

1. 经管理员确认后解决 Host UDP receive buffer。
2. 检查 Go2 有线网卡、路由、ROS_DOMAIN_ID 和 CycloneDDS。
3. 运行 XT16 preflight。
4. 检查 raw odom、标准 odom 和 TF。
5. 确认没有重复节点或 TF publisher。

### P2：重建 no-motion 基线

1. build-go2-image。
2. build-lego 和 20 秒静止 mapping。
3. build-navigation 和 20 秒 navigation-dry-run。
4. 验证地图、TRACKING、全局路径和安全速度链。
5. 运行墙体安全配置和合成墙测试。

### P3：冻结地图与路线

1. 在北站公园重新测量时间同步。
2. 录制/建图并检查地面、墙体、低障碍和人员伪影。
3. 保存地图 provenance。
4. 人工录制公园主路线和回收路线。
5. 检查路线与地图 hash、坡度、台阶、禁行区和接管点。

### P4：分级现场验收

1. dry-run。
2. 架空或轮廓隔离的轴向/StopMove 验证。
3. 平地短距离受监督测试。
4. 障碍停车与绕行。
5. 坡道单段。
6. 楼梯单段，仅在硬件能力、步态和安全条件明确后。
7. 完整路线。

### P5：业务闭环

实现机械臂和任务状态机后，再进行空杯、配重、常温水和实际咖啡的逐级测试。导航通过不能替代机械臂互锁、防洒、电量、通信、订单和人工接管验收。

## 10. 常见故障

### 10.1 Docker 中看不到 Go2 topic

按顺序查：

    ip -br addr
    ip route get 192.168.123.18
    ./scripts/check_go2_dds_receive_buffers.sh
    GO2_NET_IFACE=<有线网卡> \
      ./scripts/dddmr_docker_go2_xt16.sh preflight --samples 3 --timeout 10

只有 /parameter_events 和 /rosout 时，优先怀疑 DDS 网卡、路由、ROS domain 或接收缓冲区。

### 10.2 LeGO-LOAM 崩溃

优先检查：

- 是否混用 Host 和 Docker PCL/GTSAM。
- 点数与 num_horizontal_scans 是否一致。
- 点云字段、frame、ring 和 timestamp 是否有效。
- TF 是否可查。
- loop closure/OMP 过滤是否被意外打开。
- 是否有重复 mapping container。

保留短 bag、完整配置、镜像标签、启动命令和 crash log。

### 10.3 地面空心

1. 机器人保持静止。
2. 检查 patch_first_ring_to_baselink=true。
3. 检查 base_link 到 LiDAR 外参。
4. 调 ground_fov_bottom/top 和正负方向范围。
5. 第一帧正确后再慢速移动。

### 10.4 定位不进入 TRACKING

检查：

- mapcloud/mapground/key poses 是否完整。
- live feature cloud 是否新鲜。
- odom 是否新鲜且轴向正确。
- 地图与现场是否重叠。
- localization_quality、主模态质量和 XY/yaw spread。
- 是否处于重复走廊等模糊几何。

不要通过随意放宽阈值强行进入 TRACKING。

### 10.5 没有全局路径

检查：

- pose_graph_dir 是否存在。
- 起点和目标是否靠近有效 ground。
- weighted_ground 是否连通。
- 静态墙体半径是否把起点隔离。
- dGraph 和 global marking 是否覆盖入口。
- map/localization 是否错位。

### 10.6 有路径但速度为零

检查：

- /dddmr_go2/p2p_decision 当前状态。
- localization_status 是否 TRACKING 且新鲜。
- local planner odom 是否正确。
- collision critics 是否拒绝全部轨迹。
- segmented_cloud_pure 是否包含机器人自身/地面。
- 速度是否只被 remap 到 dry-run topic。

速度为零可能是正确的 fail-closed 行为，不应先绕过 gate。

### 10.7 RViz 不显示

    echo "$DISPLAY"
    xhost +local:docker
    RVIZ=true ./scripts/dddmr_docker_go2_xt16.sh navigation-dry-run

优先用 X11 会话排除 Wayland 转发问题。

### 10.8 地图路径错误

    rg -n 'pose_graph_dir' \
      src/dddmr_beginner_guide/config/go2_xt16_navigation.yaml
    ls -la "$DDDMR_BAGS_DIR"

YAML 中使用容器路径 /root/dddmr_bags/<map>；Host 必须存在 $DDDMR_BAGS_DIR/<map>。

## 11. 检查与测试

### 11.1 仓库基础检查

从仓库根目录：

    cd "$REPO_ROOT"
    ./scripts/check-repository.sh

提交前：

    git add -- 项目交接文档.md
    ./scripts/check-repository.sh --staged

### 11.2 无运动安全检查

    cd "$DDDMR_WS"
    python3 scripts/check_go2_xt16_wall_safety_config.py
    ./scripts/check_go2_xt16_acceptance_pipeline_selftest.sh
    ./scripts/check_go2_xt16_report_completion.sh --allow-incomplete
    ./scripts/run_go2_xt16_recorded_route_supervised_live.sh --check

其中 acceptance pipeline selftest 只证明证据工具链可用，不代表真实机器人验收通过。

### 11.3 相关单元测试范围

代码中已有以下重点测试：

- LeGO-LOAM mapping math 和 receipt synchronization。
- MCL 状态机、posterior mode、自适应粒子策略和粒子滤波 posterior。
- Local Planner 状态仲裁、原地旋转迟滞和碰撞 horizon。
- 静态/动态走廊模型。
- P2P 连续失败 debounce。
- 路线 tracker 和 route_tool。
- Go2 command gate、pointcloud freshness gate、DDS buffer checker。

修改算法后应在 Docker 构建对应包并运行 colcon test，再用 colcon test-result --all --verbose 检查。不要只看 build 成功。

### 11.4 现场证据最小集合

每次实机测试保存：

- branch、commit SHA、Git dirty 状态。
- Docker image ID。
- 地图目录和 map/ground/poses/edges hash。
- 路线 JSON hash。
- 所有相关 YAML。
- 启动命令和环境变量。
- preflight 输出。
- TF、odom、localization 状态。
- global path、dGraph、obstacle marking、cmd_vel、Sport request bag。
- 最大速度、最小障碍间距、终点位置/航向误差、耗时。
- StopMove 和清理结果。
- 成功/失败原因，失败 bag 也必须保留。

## 12. 停止与收尾

点到点导航：

    ./scripts/run_go2_xt16_navigation_test.sh --stop

录制路线：

    ./scripts/run_go2_xt16_recorded_route_dry_run.sh --disable
    ./scripts/run_go2_xt16_recorded_route_dry_run.sh --stop

收尾检查：

    docker ps --format '{{.Names}} {{.Status}} {{.Image}}'
    ps -ef | rg 'sport_cmd_vel|p2p_move_base|recorded_route'

真实运动结束必须确认：

- adapter 已发布 StopMove。
- Docker 速度源和 Host adapter 均退出。
- /api/sport/request 没有本机残留 publisher。
- 没有残留目标或 enabled route controller。
- 日志和 summary 已保存。

## 13. Git 工作流

仓库要求：

1. 开始前运行 git status --short --branch。
2. 只修改任务所需文件。
3. 不提交 bags、PCD、rosbag、运行日志、build/install/log、模型大文件或凭据。
4. 显式暂存路径。
5. 运行基础检查和相关测试。
6. 使用 helper 提交和推送功能分支。

示例：

    cd "$REPO_ROOT"
    ./scripts/submit-change.sh "Document project handover" -- 项目交接文档.md

helper 会拒绝 main/master，执行 staged 检查、提交并推送。若 push 失败，保留本地提交并报告，不要 force-push。

## 14. 关键参考

- [仓库规则](AGENTS.md)
- [导航工作区安全规则](dddmr_navigation/AGENTS.md)
- [Ubuntu 22.04 + Humble 运行指南](dddmr_navigation/docs/ubuntu22_humble_go2_xt16_run_guide.md)
- [Docker 快速启动](dddmr_navigation/docs/go2_xt16_docker_quickstart.md)
- [技术框架与推进路线](dddmr_navigation/docs/go2_xt16_technical_document_and_route.md)
- [全局重定位](dddmr_navigation/docs/go2_xt16_global_relocalization.md)
- [口部地面融合说明](dddmr_navigation/docs/go2_xt16_mouth_ground_fusion_notes.md)
- [录制路线巡航](dddmr_navigation/docs/go2_xt16_recorded_route_navigation.md)
- [Sport 实机运行手册](dddmr_navigation/docs/go2_xt16_sport_live_runbook.md)
- [导航安全评审](dddmr_navigation/docs/go2_xt16_navigation_review_20260710.md)
- [墙穿越案例](dddmr_navigation/docs/go2_xt16_wall_crossing_case_20260710.md)
- [轴向与倾斜验收清单](dddmr_navigation/docs/go2_xt16_axis_tilt_acceptance_checklist.md)
- [离线建图说明](dddmr_lego_loam_offline_mapping_notes.md)

## 15. 最终交接清单

### 源码与环境

- [ ] 已记录 branch、commit 和 dirty files。
- [ ] Docker 两层镜像可重建。
- [ ] Unitree Host ROS 环境路径已确认。
- [ ] GO2_NET_IFACE、GO2_DDS_IP 和 ROS_DOMAIN_ID 已确认。
- [ ] DDS receive buffer 检查通过。

### 数据与地图

- [ ] bags、地图、路线和日志已独立备份。
- [ ] 当前地图四个主 PCD 和 keyframe 目录完整。
- [ ] 当前路线与地图 hash 一致。
- [ ] 地图配置、同步测量和生成日志可追溯。

### 无运动验证

- [ ] XT16 preflight 通过。
- [ ] TF、raw odom、standard odom 正确。
- [ ] 20 秒 mapping 不崩溃且地面不空心。
- [ ] navigation dry-run 中定位进入 TRACKING。
- [ ] 真实 /api/sport/request 未被创建。
- [ ] 路径不穿墙，局部规划输出合理。
- [ ] 退出后无残留容器和 adapter。

### 实机与业务

- [ ] 有明确运动批准、操作员、安全员和急停。
- [ ] 低速 Sport probe 与 StopMove 通过。
- [ ] 平地短目标重复通过并记录误差。
- [ ] 障碍、坡道、台阶和跌落按级别验收。
- [ ] 公园主路线和回收路线冻结。
- [ ] 机械臂、杯检、互锁和客户确认实现。
- [ ] 空载、配重、常温水、咖啡逐级测试完成。
- [ ] 已知限制和不放行条件书面确认。

完成以上清单前，项目的正确表述是“Go2 + XT16 三维导航研发与受监督验证平台”，不是“可无人值守交付的咖啡配送系统”。
